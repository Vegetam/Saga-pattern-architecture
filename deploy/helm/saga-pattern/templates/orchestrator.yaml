apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "saga-pattern.fullname" . }}-orchestrator
  labels:
    {{- include "saga-pattern.labels" . | nindent 4 }}
    app.kubernetes.io/component: orchestrator
spec:
  replicas: {{ .Values.replicas.orchestrator }}
  selector:
    matchLabels:
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: orchestrator
  template:
    metadata:
      labels:
        {{- include "saga-pattern.labels" . | nindent 8 }}
        app.kubernetes.io/component: orchestrator
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      initContainers:
        - name: wait-kafka
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              set -eu
              BROKER="$(echo "$KAFKA_BROKERS" | cut -d, -f1)"
              HOST="$(echo "$BROKER" | cut -d: -f1)"
              PORT="$(echo "$BROKER" | cut -d: -f2)"
              echo "Waiting for Kafka at $HOST:$PORT";
              until nc -z "$HOST" "$PORT"; do sleep 2; done
          envFrom:
            - configMapRef:
                name: {{ include "saga-pattern.fullname" . }}-config
        - name: wait-db
          image: postgres:15-alpine
          command: ["sh","-c","until pg_isready -d \"$DATABASE_URL\"; do echo waiting for db; sleep 2; done"]
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "saga-pattern.fullname" . }}-secrets
                  key: ORCHESTRATOR_DATABASE_URL
        {{- if eq .Values.config.sagaStateStore "redis" }}
        - name: wait-redis
          image: redis:7-alpine
          command: ["sh","-c","until redis-cli -u \"$REDIS_URL\" ping | grep -q PONG; do echo waiting for redis; sleep 2; done"]
          env:
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "saga-pattern.fullname" . }}-secrets
                  key: REDIS_URL
        {{- end }}
      containers:
        - name: orchestrator
          image: "{{ .Values.images.orchestrator.repository }}:{{ .Values.images.orchestrator.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: 3000
          envFrom:
            - configMapRef:
                name: {{ include "saga-pattern.fullname" . }}-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "saga-pattern.fullname" . }}-secrets
                  key: ORCHESTRATOR_DATABASE_URL
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "saga-pattern.fullname" . }}-secrets
                  key: REDIS_URL
          livenessProbe:
            httpGet:
              path: /live
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}

---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "saga-pattern.fullname" . }}-orchestrator
  labels:
    {{- include "saga-pattern.labels" . | nindent 4 }}
    app.kubernetes.io/component: orchestrator
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: orchestrator
  ports:
    - name: http
      port: 80
      targetPort: http
